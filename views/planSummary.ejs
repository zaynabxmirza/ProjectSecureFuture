<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Your Plan Summary</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="/css/styles.css">
  </head>
<body> 
<%- include('../partials/userNavbar') %>

<div class="container mt-5">
  <div class="card shadow p-4 bg-light">
    <h2 class="mb-4 text-primary"> Your Personalised Financial Plan</h2>
    
    <% if (plan.date) { %>
      <p class="text-muted">Generated on: <%= new Date(plan.date).toLocaleDateString() %></p>
    <% } %>
    
    <p class="lead mb-4">
      Here's a custom plan based on the information you have shared. Take your time to go through it and begin taking small steps toward your financial goals.
    </p>

    <h4 class="text-success"> Recommendations</h4>
    <% if (plan.recommendations.length > 0) { %>
      <ul class="list-group mb-4">
        <% plan.recommendations.forEach(function(item) { %>
          <li class="list-group-item"><i class="bi bi-check-circle-fill text-success me-2"></i> <%= item %></li>
        <% }); %>
      </ul>
    <% } else { %>
      <p class="text-muted">No recommendations were generated.</p>
    <% } %>

    <h4 class="text-info"> Next Steps</h4>
    <% if (plan.nextSteps.length > 0) { %>
      <ol class="list-group list-group-numbered mb-4">
        <% plan.nextSteps.forEach(function(step) { %>
          <li class="list-group-item"><%= step %></li>
        <% }); %>
      </ol>
    <% } else { %>
      <p class="text-muted">No specific next steps found.</p>
    <% } %>

    <div class="text-end mt-4 d-flex justify-content-between">
      <a href="/user_dashboard" class="btn btn-outline-secondary">⬅ Back to Dashboard</a>
      <a href="/questionnaire/personal-information" class="btn btn-success">Start New Plan</a>
    </div>
    
  </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>

</body>
</html>