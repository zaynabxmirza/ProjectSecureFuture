<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Update Plan</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
  <%- include('../partials/adminNavbar') %>

  <div class="container mt-5">
    <h3>Edit Plan Output for <%= user.userforename %> <%= user.usersurname %></h3>
    <p class="text-muted">Original user inputs (age, income, housing, etc.) are not editable.</p>

    <form action="/admin/userplans/update/<%= user._id %>/<%= planIndex %>" method="POST">

      <div class="mb-4">
        <label for="recommendations" class="form-label"><strong>Recommendations (one per line)</strong></label>
        <textarea id="recommendations" name="recommendations" rows="5" class="form-control"><%= plan.plan.recommendations.join('\n') %></textarea>
      </div>

      <div class="mb-4">
        <label for="nextSteps" class="form-label"><strong>Next Steps (one per line)</strong></label>
        <textarea id="nextSteps" name="nextSteps" rows="4" class="form-control"><%= plan.plan.nextSteps.join('\n') %></textarea>
      </div>

      <button type="submit" class="btn btn-primary">Update Plan</button>
      <a href="/admin/userplans" class="btn btn-secondary ms-2">Cancel</a>
    </form>
  </div>
</body>
</html>